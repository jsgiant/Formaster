import React, { PureComponent } from 'react'
import Header from '../../../Common/components/Header/Header'

class UserDashboard extends PureComponent {
   render() {
      return <Header onLogoutClick={() => {}} />
   }
}

export { UserDashboard }
